import 'package:flutter/material.dart';

class Etape2 extends StatelessWidget {
  const Etape2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('etape2'),),
    );
  }
}