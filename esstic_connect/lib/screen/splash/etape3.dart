import 'package:flutter/material.dart';

class Etape3 extends StatelessWidget {
  const Etape3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('etape3'),),
    );
  }
}